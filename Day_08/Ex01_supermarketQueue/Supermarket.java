public class Supermarket implements PersonQueue
{
	Person Head;
	/**
	*Adds another person to the queue
	*/


	}

	void insert (Person person) {
		addPerson(Person p);
	}
	/**
	* Removes a person form the queue
	*/

	Person retrieve() {
		servePerson()
	}
	
	void addPerson(Person p) {
		if (head == null)
			head = p;
		else 
		{
			head.add(p);
		}
	
	Person servePerson() {

	}
	void printQueue() {
		Person temp = Head;
			while (head != null) {
				System.out.println (temp);
				temp = temp.next;
	}
}