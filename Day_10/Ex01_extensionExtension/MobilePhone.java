public class MobilePhone extends OldPhone {
	private String[] recentlyCalled = new String[10];
	private int callCounter;

		/**
		* Activates the alarm and displays the time
		* @param alarmTime is the default set time.
		*/
		public void ringAlarm(String alarmTime) {
			System.out.println("Alarm, time is now: " + alarmTime);		
		}

		/**
		* Start a game 
		* @param load the game and display the name
		*/
		public void playGame(String gameName) {
			System.out.println("Loading " + gameName + "...");		
		}
		/** 
		* Print the last 10 numbers called.
		* @return the last 10 numbers on new lines.
		*/
		public String printLastNumbers() {
			for(int i = 0; i <= )
			String 
		}

	/*	public void call(String number) {
			super.call(number);
			storeLastDialed(number);
			} 	

		} 
*/


} 